<!DOCTYPE html>
<html lang="en">
<head>
<meta name="viewport" content="width=device-width, initial-scale=1">
 <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login In</title>
    <link rel="stylesheet" href="/public/stylesheets/formPage.css">
    

</head>
<body>


<div id="error" class ="error" hidden></div>
<div id = "closebtnId" class="closebtn" hidden> &times;</div>
{{#if hasErrors}}
    <p id = "error_alert" class="error">{{error}}</p>
     <div id = "closebtnId1" class="closebtn"> &times;</div>
{{/if}}
<div class="">
<form id ="loginForm" style="text-align: left; margin-left: 500px;" form action="/reservation/new" method="POST" >
<h1>Book Appointment</h1> 
        <label for="fname">Patient's First Name:</label>
            <input  type="text" name="fname" id="fname"></p>
            <p>
            <label for="lname">Patient's Last Name:</label>
            <input  type="text" name="lname" id="lname"></p>
            <p>
            <label for="email">Patient's Email:</label>
            <input  type="email" name="email" id="emailid"></p>
            <p>
            <label for="splist">Specialism:</label>
            <select name="specialism" onchange="updatedoc(this)" id="splist">
              <option value=""> Select a Specialism...</option>
              {{#each spList}}
              <option value="{{this}}">{{this}}</option>
              {{/each}}
            </select>
            <p>
            <label for="drlist">Doctor:</label>
            <select name="doctor_id" id="drlist" required>
              <option value=""> --Select a Hospital--</option>
              {{#each doctorList}}
              <option value="{{this._id}}">{{this.fname}} {{this.lname}}</option>
              {{/each}}
            </select></p>
            <p>
              <label for="resvDate">Date</label>
              <input type="date" name="app_date" id="resvDate" value="" required></p>
            
            <br/>
            <p><button type="submit" class="button1">Schedule</button></p>
     </form>
      </div>
    <script>DateRestrict();</script>



</body>



</html>
